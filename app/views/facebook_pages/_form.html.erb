<%= form_with(model: facebook_page, local: true) do |form| %>
  <% if facebook_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facebook_page.errors.count, "error") %> prohibited this facebook_page from being saved:</h2>

      <ul>
      <% facebook_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% @fetched_pages.each do |page| %>
    <h3><%= page["id"]%>: <%= page["name"] %></h3>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :access_token %>
    <%= form.text_field :access_token %>
  </div>

  <div class="field">
    <%= form.label :organization_id %>
    <%#= form.text_field :organization_id, value: @current_organization.id %>
    <%= form.text_field :organization_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
